# 任务交接：交互式原型工具栏复核

## 背景

用户在查看 `高保真原型_交互式原型说明_v1_20260608.html` 时反馈：右下角“交互式原型说明 / 新增标注 / 隐藏标注 / 当前展示端 / 选择 HTML 文件 / 导出新版”工具栏有时会遮挡原型内容，需要增加隐藏、打开和拖动交互，并把规则沉淀到可复用 skill。

主控已纠偏：本轮不再由主控直接继续实现原型，改由 `07 - 高保真原型助手` 正式复核接管。主控负责派发、验收、状态维护和后续备份触发。

## 目标

请 `07 - 高保真原型助手` 复核并正式接管本轮工具栏遮挡问题，确认当前临时修改是否完整可用；如不完整，只在原型实现职责范围内修复，并按项目文件命名规则输出正式结果或问题记录。

## 用户原话

```text
增加一个 隐藏和打开和拖动的交互 ，这个页面有时候会遮挡原型内容 修改完记得在 skill 上对规则进行相应的调整，确保以后能直接复用
```

用户随后指出：

```text
作为大统领，我发现你现在的工作都是自己在弄，没有分配给对应的助手安排他们工作，你这样会对上下文的消耗非常严重
```

## 主控理解

- 这是 HTML 原型工具层的交互问题，归属 `07 - 高保真原型助手`。
- 本轮只处理“标注说明工具栏遮挡原型内容”的实现与验证，不合并主原型和交互式说明原型。
- 当前主控此前已做过临时修改，07 需要复核接管，不能默认视为已验收。
- skill 规则已被临时更新，07 需要检查规则是否足以复用，并在回报中说明是否还需补充。

## 必读文件

- `/Users/xuyunfeng/Documents/k12/AGENTS.md`
- `/Users/xuyunfeng/Documents/k12/00_项目规则/项目总览_工作流程_v1_20260602.md`
- `/Users/xuyunfeng/Documents/k12/00_项目规则/项目主控调度规则_v1_20260602.md`
- `/Users/xuyunfeng/Documents/k12/00_项目规则/助手向主控回报规则_v1_20260603.md`
- `/Users/xuyunfeng/Documents/k12/00_项目规则/阶段自动交接规则_v1_20260603.md`
- `/Users/xuyunfeng/Documents/k12/13_任务卡片/原型设计助手_任务说明_v1_20260602.md`
- `/Users/xuyunfeng/Documents/k12/10_高保真原型/高保真原型_交互式原型说明_v1_20260608.html`
- `/Users/xuyunfeng/.codex/skills/interactive-prototype-doc/SKILL.md`

## 输入来源

- 浏览器页面：`http://127.0.0.1:8123/高保真原型_交互式原型说明_v1_20260608.html?verify=toolbar`
- 当前页面元素：`div#annotationToolbar`
- 用户浏览器评论：工具栏遮挡原型内容，需要隐藏、打开、拖动。

## 任务目标

1. 复核当前 HTML 是否已经包含工具栏收起、展开、拖动、可视区限制和状态持久化能力。
2. 使用浏览器验证工具栏交互是否真实可用，而不仅是静态代码存在。
3. 验证原有标注能力不被破坏，包括新增标注、隐藏标注、选择 HTML 文件、导出新版、标注拖动、标注弹窗编辑。
4. 检查 `interactive-prototype-doc` skill 中是否已经沉淀“标注控制区必须可移动、可收起、可展开、持久化”的复用规则。
5. 如发现当前实现不完整，在 07 职责范围内修复；如需要生成新版 HTML，按命名规则输出到 `10_高保真原型/`。

## 输出目录

- `/Users/xuyunfeng/Documents/k12/10_高保真原型/`

## 建议文件名

如只复核并确认当前文件可用，可更新或新增问题记录：

- `高保真原型_交互式原型问题记录_v1_20260610.md`

如需要输出新版 HTML：

- `高保真原型_交互式原型说明_v2_20260610.html`

是否生成新版由 07 复核结果决定；不得为制造版本而复制文件。

## 必须遵守的已确认规则

- 原型交付标准是可交互 HTML。
- HTML 原型、原型问题、状态验证归属 07。
- 阶段助手完成、阻塞、发现需确认或发现职责边界外问题时，必须按“主控回报”格式回报主控。
- 发现任何需要用户确认的问题，必须先回报主控，不得只写入文件；主控会统一向用户确认。
- 主控后续验收通过后再触发 `10 - 备份助手`，07 不自行派发备份。

## 禁止事项

- 不得合并 `高保真原型_全端合并离线主版HTML_v2_20260605.html` 与本交互式说明文件；合并主原型另立任务。
- 不得重做业务页面、不改需求、不改信息架构、不改数据契约。
- 不得删除现有 `prototypeData`、页面计划、标注数据结构、导入导出能力。
- 不得把浏览器评论层造成的点击拦截误判为原型自身问题；如受影响，需要说明验证限制。

## 验收标准

- 工具栏可以一键收起和展开。
- 工具栏可以拖动，并自动限制在当前可视区域内。
- 刷新页面后保留上次收起状态和位置。
- 原有功能不丢失：新增标注、隐藏标注、选择 HTML 文件、导出新版、标注拖动/编辑仍可用。
- 浏览器控制台无明显 JavaScript 报错。
- 若 Codex 浏览器评论层影响点击验证，必须在回报中说明，并补充无评论层验证方式或明确风险。

## 完成后回报要求

请按以下格式回报主控：

```text
主控回报

当前状态：
已完成 / 进行中但阻塞 / 需要确认 / 需要回流

产物文件：
列出已生成或已复核文件路径；没有新产物时说明原因。

关键结论：
说明工具栏收起、展开、拖动、持久化、原功能回归验证结果。

待确认问题：
列出需要用户或上游确认的问题；没有则写“当前无阻塞确认项”。

风险与依赖：
列出浏览器验证限制、评论层影响、skill 规则仍需补充等风险。

建议下一步：
建议主控验收 / 需要继续修复 / 需要回流某助手 / 触发 10 号备份助手。

下游交接包：
建议下游助手：
下游输入文件：
已确认结论：
待确认问题：
风险与依赖：
禁止下游误用：

上下文交接：
是否需要接替：
旧助手名称：
建议新助手名称：
交接文件路径：
```

## 风险与依赖

- 当前浏览器页面带有评论层，可能拦截工具栏点击，需要 07 判断是否需要换无评论层 URL 或直接使用本地浏览器补测。
- 主控此前临时修改过 HTML 和 skill，07 需要复核事实，不得只按主控口头说明验收。
- 如果 skill 位于项目外 `/Users/xuyunfeng/.codex/skills/`，备份策略需由主控后续判断，07 只说明变更是否满足复用规则。

## 下一步动作

- 主控将本任务包发送给 `07 - 高保真原型助手`。
- 07 回报后，主控检查产物、命名、边界和验收结果。
- 验收通过后，主控再派发 `10 - 备份助手` 执行节点备份。
