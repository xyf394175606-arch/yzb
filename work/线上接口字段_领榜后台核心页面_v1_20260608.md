# 线上接口字段_领榜后台核心页面_v1_20260608

## 背景
继续将线上领榜教育管理系统后台页面 1:1 还原到本地离线高保真原型。

## 目标
记录已通过线上接口读取到的核心页面字段和样例数据，用于后续继续校准本地原型。

## 输入来源
- 线上地址：`https://lbedu.youzuobiao.cn/#/lbedu/users/parent-ele`
- 租户：`tenant-id: 1`
- 接口前缀：`/admin-api`
- 本地响应缓存：`/tmp/lbedu_core_api_responses.json`

## 关键结论
- 线上真实菜单包含：家长管理、教师管理、需求单管理、撮合单管理、VIP订单管理、教师入驻审核、平台参数配置、评价管理、分销规则管理、分销账号列表、分销线索管理、支付订单。
- 本轮已重点校准：分销规则管理、支付订单。
- 支付订单真实接口为 `/admin-api/pay/order/page`，字段不是之前临时设定的“业务订单号/微信支付回传订单号”口径，而是支付平台通用字段：支付订单ID、商户订单号、应用名称、支付主题、支付内容、金额、状态、用户IP、创建时间、成功时间、渠道订单号。
- 分销规则真实接口为 `/admin-api/eduadmin/distribution-rule-ele/page`，规则编码包括 `TEST_TEACHER_ORDER_L1_CN`、`TEST_PARENT_TRIAL_L1_SH` 等。

## 待确认问题
- 是否继续逐页按接口真实字段替换所有本地模拟数据。
- 是否需要同步线上页面截图做像素级视觉校准。

## 风险与依赖
- 线上接口数据可能随测试库变化，后续校准应以最新接口为准。
- 离线原型仍为静态模拟，不接真实接口。

## 下一步动作
- 继续校准教师入驻审核、平台参数配置、评价管理、撮合单管理等页面的真实字段和样例数据。
- 每轮修改后继续运行已有四批校验与菜单覆盖校验。

## 2026-06-08 补充：教师入驻审核校准
- 已将教师入驻审核列表改为真实接口字段口径：审核ID、审核类型、教师姓名、手机号、申请时间、审核状态、审核人、审核时间、更新人、操作。
- 已将审核详情弹窗按 `contentSnapshot` 关键字段补充：昵称、真实姓名、出生年份、微信、所在城市、家庭住址、毕业院校、专业、教师类型、授课方式、授课科目、授课年级、授课区域、教龄、授课时间、课时费用、试讲类型、证明材料。
- 本轮恢复了第二批页面常量，避免函数替换时影响撮合单、VIP订单、平台配置、评价管理页面。

## 2026-06-08 补充：撮合单与 VIP 订单校准
- 撮合单管理已按 `/admin-api/eduadmin/matching-order-ele/page` 真实字段扩展：订单ID、订单编号、需求单编号、家长/手机、老师/手机、科目年级、课时费、信息费、信息费订单号、状态、创建时间。
- 撮合单详情已补充：需求ID、老师/家长联系方式、城市、倍率、确认时间、取消类型、取消原因等。
- VIP订单管理已按 `/admin-api/eduadmin/vip-order-ele/page` 真实字段扩展：订单ID、订单编号、家长ID、VIP类型、VIP价格、实付金额、支付订单号、支付状态、支付时间、VIP开始时间、VIP到期时间、创建时间。

## 2026-06-08 补充：用户业务与需求单校准
- 家长管理已按 `/admin-api/eduadmin/parent-ele/page` 真实字段扩展：家长ID、用户ID、手机号、姓名、昵称、微信、孩子数量、VIP状态、累计订单、完成订单、累计消费、注册时间、账号状态。
- 教师管理已按 `/admin-api/eduadmin/teacher-ele/page` 真实字段扩展：教师ID、用户ID、昵称、姓名、联系电话、教师类型、授课方式、教学科目、课时费、机构课时费、订单数、浏览数、认证状态。
- 需求单管理已按 `/admin-api/eduadmin/demand-ele/page` 真实字段扩展：需求ID、需求编号、家长ID、家长/手机号、学生ID、学生姓名、年级、科目、授课方式、城市、授课区域、预算范围、需求状态、申请数、浏览数。

## 2026-06-08 补充：分销账号与分销线索校准
- 分销账号列表已按 `/admin-api/eduadmin/distribution-user-ele/page` 真实字段扩展：账号ID、平台用户ID、邀请码、上级用户ID、关联家长、关联老师、累计佣金、已结算佣金、可提现佣金、已提现佣金、冻结佣金、直属人数、间接人数、团队人数、最近绑定时间、状态、备注、创建时间。
- 分销线索管理已按 `/admin-api/eduadmin/distribution-lead-ele/page` 真实字段扩展：线索ID、线索编号、归属人ID、客户用户ID、客户资料ID、客户姓名、客户手机号、客户身份、线索类型、线索状态、目标类型、目标对象、最近跟进时间、转化时间、创建时间。
- 分销线索详情已补充关系层级、来源类型、目标ID、最近跟进内容、备注等真实接口字段。

## 2026-06-08 补充：平台配置与评价管理校准
- 平台参数配置已按 `/admin-api/eduadmin/config-ele/page` 真实字段补齐：配置ID、配置分组、参数名称、键值类型、参数键名、参数键值、默认值、排序、变量描述、是否系统默认、状态、创建时间。
- 平台配置修改弹窗已补齐配置ID、默认值、排序、创建时间等字段，保持列表页只展示筛选、表格、分页和行内操作。
- 评价管理已按 `/admin-api/eduadmin/evaluation-ele/page` 真实字段补齐：主键ID、关联订单ID、订单编号、家长ID/姓名、教师ID/姓名、评分、评价标签、评价内容、是否匿名、是否展示、教师回复内容、教师回复时间、创建时间。
- 评价管理新增“查看详情”弹窗，删除仍使用独立确认弹窗，不在列表页常驻展示详情或确认内容。

## 2026-06-08 补充：撮合单操作权限校准
- 根据线上权限 `eduadmin:matching-order-ele:update` 和 `eduadmin:matching-order-ele:refund`，撮合单管理列表操作从单一“详情”扩展为“详情 / 修改 / 退款”。
- 新增“修改撮合单”弹窗，覆盖订单状态、信息费订单号、确认时间、取消类型、取消原因、后台备注等可编辑字段。
- 新增“撮合单退款处理”弹窗，覆盖订单、家长、老师、信息费订单号、可退信息费、退款金额、退款原因、退款说明。
- 退款弹窗只做高保真原型交互，不调用真实支付退款接口。

## 2026-06-08 补充：家长与需求单更新权限校准
- 根据线上权限 `eduadmin:parent-ele:update`，家长管理列表新增“编辑”操作，并新增“编辑家长资料”弹窗。
- 家长编辑弹窗覆盖：家长ID、用户ID、手机号、家长姓名、昵称、微信号、年级、性别、家庭住址、学校、VIP状态、账号状态、后台备注。
- 根据线上权限 `eduadmin:demand-ele:update`，需求单管理列表新增“修改”操作，并新增“修改需求单”弹窗。
- 需求单修改弹窗覆盖：需求ID、需求编号、家长ID、家长姓名、手机号、需求状态、学生姓名、学生年级、需求科目、授课方式、城市、授课区域、授课地址、预算上下限、教师要求、后台备注。

## 2026-06-08 补充：导出权限入口校准
- 根据线上权限 `export`，已为家长管理、教师管理、需求单管理、撮合单管理、VIP订单管理、支付订单补齐“导出”入口。
- 导出按钮统一打开“导出确认”弹窗，展示导出范围、导出格式、权限来源和原型边界。
- 原型不生成真实下载文件，避免离线高保真页面产生副作用。

## 2026-06-08 补充：分销规则新增与编辑权限拆分
- 根据线上权限 `eduadmin:distribution-rule-ele:create` 和 `eduadmin:distribution-rule-ele:update`，分销规则管理已拆分“新增规则”和“编辑规则”两个弹窗。
- 顶部“新增规则”打开新增弹窗，行内“编辑”打开编辑弹窗，不再复用同一个“新增规则”弹窗。
- 编辑弹窗使用真实规则样例 `TEST_TEACHER_ORDER_L1_CN / 老师成单一级返佣`，与线上规则列表字段保持一致。

## 2026-06-08 补充：支付订单详情字段补齐
- 支付订单详情已按 `/admin-api/pay/order/page` 真实字段继续补齐：应用ID、支付订单号、扩展单ID、渠道费率、渠道费用、渠道ID、渠道编码、渠道用户ID、回调地址、过期时间。
- 详情弹窗重新分组为基础信息、支付内容、金额信息、渠道信息、回调与时间，提升与线上支付订单详情字段口径的一致性。

## 2026-06-08 补充：教师详情字段补齐
- 教师详情已按 `/admin-api/eduadmin/teacher-ele/page` 真实字段继续补齐：用户ID、微信号、出生年份、头像、省市区编码与名称、经纬度、GeoHash、授课区域编码/名称、授课时间配置、专长标签、视频简历、职业学校、专业职称、驳回原因、是否激活、评价数、收藏次数、月收入、余额、冻结余额、邀请人ID/姓名、最近活跃时间、最近成单时间。
- 教师详情弹窗按基础信息、地区与定位、教学与资质、认证与驳回、运营与财务数据、关系与时间重新分组。

## 2026-06-08 补充：家长详情字段补齐
- 家长详情已按 `/admin-api/eduadmin/parent-ele/page` 真实字段继续补齐：昵称、真实姓名、性别、头像、省市区编码与名称、经纬度、GeoHash、VIP购买时间、VIP到期时间、孩子数量、默认孩子ID、最近发布需求时间。
- 家长详情弹窗按基础信息、地区与定位、VIP与订单、孩子与需求重新分组。

## 2026-06-08 补充：需求单详情字段补齐
- 需求单详情已按 `/admin-api/eduadmin/demand-ele/page` 真实字段继续补齐：学生ID、科目编码、年级编码、授课方式值、省市区编码与名称、状态值、信息费、并发数、性别要求值、经验要求。
- 详情弹窗保留业务分组，并将预算与信息费、状态与统计字段补齐。

## 2026-06-08 补充：撮合单详情字段补齐
- 撮合单详情已按 `/admin-api/eduadmin/matching-order-ele/page` 真实字段继续补齐：老师ID、家长ID、城市编码、老师收入、平台费用、试讲时间、企微群ID、完成时间、创建时间、更新时间、状态值、取消类型值。
- 详情弹窗重新分组为订单基础信息、老师与家长、课程与城市、费用拆分、企微与时间、取消信息。

## 2026-06-08 补充：VIP订单详情字段补齐
- VIP订单详情已按 `/admin-api/eduadmin/vip-order-ele/page` 真实字段继续补齐：VIP类型值 `vipType`、支付状态值 `payStatus`。
- 详情弹窗重新分组为订单基础信息、VIP权益、支付信息。

## 2026-06-08 补充：教师入驻审核详情字段补齐
- 教师入驻审核详情已按 `/admin-api/eduadmin/teacher-audit-record-ele/page` 真实字段继续补齐：教师ID、审核类型值、审核状态值、旧资料、驳回原因、审核人ID、更新时间、教师学校/专业/职业学校/职称。
- `contentSnapshot` 继续补齐：头像URL、证明材料URL、省市区编码与名称、授课省市区编码与名称、经纬度、教师类型值、授课方式值、授课科目编码、授课年级编码、教龄值、授课时间配置标记、试讲类型值、教学介绍、视频简历。
- 审核详情弹窗重新分组为接口元信息、审核对象、基础快照、地区编码、授课信息、价格与试讲、自我介绍与证明、审核结果。

## 2026-06-08 补充：分销账号与线索详情字段补齐
- 分销账号列表新增“查看详情”操作，详情弹窗补齐 `lastCommissionTime`、状态值、嵌套 `parent` / `teacher` 信息中的老师ID、老师用户ID、昵称、真实姓名、手机号、头像。
- 分销线索详情继续补齐：客户头像、客户身份值、线索类型名称、线索状态值。
- 分销账号详情按账号基础信息、佣金数据、团队数据、关联家长、关联老师分组展示。

## 2026-06-08 补充：分销规则弹窗字段补齐
- 分销规则新增/编辑弹窗已按 `/admin-api/eduadmin/distribution-rule-ele/page` 真实字段继续补齐：规则ID、触发身份值、地域类型值、省市区编码、事件类型真实值、计算模式值、基数类型值、最小基数、最高返佣、状态值、备注、创建时间。
- 新增/编辑弹窗按基础规则、地域范围、计算方式、时间与备注分组。

## 2026-06-08 补充：配置与评价原始值字段补齐
- 平台配置弹窗补齐 `isDefault`、`status` 的原始布尔值展示：是否默认原值、状态原值。
- 评价详情补齐 `isAnonymous`、`isVisible` 的原始枚举值展示：是否匿名值、是否展示值。

## 2026-06-08 补充：教师编辑弹窗字段补齐
- 教师编辑弹窗已按 `eduadmin:teacher-ele:update` 真实字段继续补齐：教师ID、用户ID、微信号、头像URL、省市区编码与名称、详细住址、经纬度、GeoHash、授课科目编码、授课区域编码/名称、授课时间配置、专长标签、视频简历、毕业院校、专业、职业学校、专业职称、认证状态、是否激活、账号状态、驳回原因。
- 编辑弹窗按基础信息、地区定位、教学资质、状态与备注分组。

## 2026-06-08 补充：家长与需求编辑弹窗字段补齐
- 家长编辑弹窗继续按 `/admin-api/eduadmin/parent-ele/page` 和 `parent-ele:update` 口径补齐：头像URL、省市区编码与名称、经纬度、GeoHash、孩子数量、默认孩子ID、VIP购买时间、VIP到期时间、订单数、完成订单数、累计消费、最近发布需求时间、注册时间。
- 需求单编辑弹窗继续按 `/admin-api/eduadmin/demand-ele/page` 和 `demand-ele:update` 口径补齐：需求状态值、发布时间、并发数、学生ID、年级编码、学段、科目编码、授课方式值、省市区编码与名称、上课频次、期望时间、信息费、信息费订单号、性别要求值、经验要求、浏览数、报名数、成功报名数。
- 编辑弹窗保持 SaaS 后台结构：可编辑字段用输入控件，只读统计字段用信息展示，不与列表页混排。

## 2026-06-08 补充：撮合单修改与退款弹窗字段补齐
- 撮合单修改弹窗继续按 `/admin-api/eduadmin/matching-order-ele/page` 和 `matching-order-ele:update` 口径补齐：需求ID、需求编号、订单状态值、老师ID、老师姓名、老师手机号、家长ID、家长姓名、家长手机号、科目、年级、城市编码、课时费、倍率、信息费、信息费订单号、老师收入、平台费用、试讲时间、企微群ID、确认时间、完成时间、创建时间、更新时间、取消类型值。
- 撮合单退款弹窗继续按 `matching-order-ele:refund` 处理场景补齐退款对象、需求/老师/家长关联信息、费用核对、订单状态值、取消类型值、企微群ID、确认/完成/更新时间、取消原因和退款说明。
- 两个处理弹窗仍保持按钮触发，不在列表页默认展示处理表单。

## 2026-06-08 补充：支付订单详情原始字段补齐
- 支付订单详情继续按 `/admin-api/pay/order/page` 真实字段补齐并重排：支付单号 `no`、支付状态值 `status`、支付金额原始分值 `price`、渠道费用原始分值 `channelFeePrice`、退款金额原始分值 `refundPrice`、渠道订单号 `channelOrderNo`。
- 详情弹窗分组调整为应用与订单、支付内容、金额信息、渠道信息、时间信息，保留人民币展示同时补充接口原始值，便于与线上后台和支付回传字段口径一致。

## 2026-06-08 补充：分销规则新增编辑原始字段口径对齐
- 分销规则新增/编辑弹窗继续按 `/admin-api/eduadmin/distribution-rule-ele/page` 字段顺序对齐：`ruleCode`、`ruleName`、`inviteeIdentityType`、`regionType`、`regionName`、`provinceCode`、`cityCode`、`districtCode`、`eventType`、`relationLevel`、`calcMode`、`baseAmountType`、`commissionRatio`、`commissionAmount`、`minBaseAmount`、`maxCommissionAmount`、`priority`、`startTime`、`endTime`、`status`、`remark`、`createTime`。
- 新增弹窗补充“规则ID提交后生成”和“创建时间提交后生成”；编辑弹窗补充 `id`、原始字段英文标识和返佣比例原始数值，减少与线上接口字段口径的偏差。

## 2026-06-08 补充：平台配置编辑字段口径对齐
- 平台配置编辑弹窗继续按 `/admin-api/eduadmin/sys-config-ele/page` 字段口径补齐英文标识：`id`、`group`、`name`、`type`、`key`、`value`、`defaultValue`、`sort`、`tip`、`isDefault`、`status`、`createTime`。
- 保留中文业务标签和原展示值，同时补充接口字段名，便于与线上配置管理页和接口返回结构保持一致。

## 2026-06-08 补充：评价管理详情与删除确认字段口径对齐
- 评价详情继续按 `/admin-api/eduadmin/evaluation-ele/page` 字段口径补齐英文标识：`id`、`orderId`、`orderNo`、`parentId`、`parentName`、`teacherId`、`teacherName`、`ratingScore`、`ratingTags`、`content`、`isAnonymous`、`isVisible`、`replyContent`、`replyTime`、`createTime`。
- 删除确认由简单提示升级为标准确认弹窗，补充删除对象、订单编号、家长/老师、评分、展示状态和 `evaluation-ele:delete` 权限说明，避免操作确认信息不足。

## 2026-06-08 补充：VIP订单详情字段口径对齐
- VIP订单详情继续按 `/admin-api/eduadmin/vip-order-ele/page` 字段口径补齐英文标识：`id`、`orderNo`、`parentId`、`vipType`、`vipPrice`、`payAmount`、`payStatus`、`payOrderNo`、`payTime`、`vipStartTime`、`vipEndTime`、`createTime`。
- 金额字段同时保留原始数值和业务展示值，支付状态和VIP类型保留原始枚举值与中文状态标签。

## 2026-06-08 补充：分销账号与线索详情字段口径对齐
- 分销账号详情继续按 `/admin-api/eduadmin/distribution-user-ele/page` 字段口径补齐英文标识：`id`、`memberUserId`、`inviteCode`、`inviterMemberUserId`、`totalCommission`、`settledCommission`、`withdrawableCommission`、`withdrawnCommission`、`frozenCommission`、`directCount`、`indirectCount`、`teamCount`、`lastBindTime`、`lastCommissionTime`、`status`、`remark`、`createTime`、`parent`、`teacher.id`、`teacher.userId`、`teacher.nickname`、`teacher.realName`、`teacher.phone`、`teacher.avatarUrl`。
- 分销线索详情继续按 `/admin-api/eduadmin/distribution-lead-ele/page` 字段口径补齐英文标识：`id`、`leadNo`、`ownerMemberUserId`、`customerMemberUserId`、`customerIdentityType`、`customerProfileId`、`customerName`、`customerAvatarUrl`、`customerMobileMask`、`relationLevel`、`sourceType`、`leadType`、`leadTypeName`、`leadStatus`、`leadStatusName`、`targetType`、`targetId`、`targetName`、`lastFollowTime`、`latestContent`、`convertTime`、`remark`、`createTime`。

## 2026-06-08 补充：后台表格分页视觉结构对齐
- 领榜后台通用表格分页从简化文本升级为线上后台常见结构：显示当前范围与总条数、每页条数选择、上一页/下一页按钮、页码、省略号、跳页输入。
- 领榜后台通用表格操作列增加右侧 sticky 视觉，长表格横向滚动时操作列保持可见，更接近 SaaS 后台数据表格体验。
- 本次只调整通用表格视觉结构，不改业务字段、不恢复页面状态测试工具、不影响移动端。

## 2026-06-08 补充：筛选区与表格工具栏视觉对齐
- 领榜后台筛选区按钮统一改为右侧操作组 `lb-filter-actions`，顺序调整为“搜索、重置、收起”，更接近线上后台常见筛选表单操作顺序。
- 表格工具栏拆分为业务操作区 `lb-toolbar-actions` 和图标工具区 `lb-toolbar-icons`，图标按钮补充搜索、刷新、全屏、列设置 title。
- 按钮 hover 态补充蓝色边框与文字反馈；本次只调整视觉结构，不改变业务数据和弹窗逻辑。

## 2026-06-08 补充：领榜后台顶部栏与页签栏真实结构对齐
- 领榜后台外壳从 `main::before/after` 伪元素硬编码顶栏，升级为真实 DOM：`lb-admin-topbar`、`lb-breadcrumb`、`lb-admin-tabs`、`lb-admin-tab`。
- 顶部栏包含菜单折叠、刷新、动态面包屑、搜索菜单/页面、暗色/全屏/通知/管理员区域；面包屑按当前菜单动态渲染为“首页 / 分组 / 当前页”。
- 页签栏按线上常用入口展示后台首页、家长管理、教师管理、教师入驻审核、需求单管理，并根据当前页面高亮。
- 本次只调整领榜后台外壳视觉结构，不恢复内部重复全端导航，不影响业务表格和弹窗逻辑。

## 2026-06-08 补充：领榜后台左侧菜单图标与选中态对齐
- 领榜后台左侧菜单由统一伪图标 `▦` 升级为真实图标结构：`lb-nav-label`、`lb-nav-icon` 和 `lbAdminIcon(id)`。
- 为后台首页、家长管理、教师管理、需求单管理、撮合单、VIP订单、审核、配置、评价、分销、支付等菜单分配独立图标，提升与线上后台菜单层级的相似度。
- 选中态补充左侧蓝色竖条、浅蓝背景、图标高亮；hover 态补充浅灰背景与蓝色文字。

## 2026-06-08 补充：家长管理列表字段顺序与列宽对齐
- 家长管理列表继续按 `/admin-api/eduadmin/parent-ele/page` 字段口径调整列表列：家长ID、用户ID、手机号、真实姓名、昵称、微信号、地区、孩子数、默认孩子ID、VIP状态、VIP到期时间、订单数、完成订单、累计消费、最近需求时间、注册时间、状态、操作。
- 通用 `lbTableCard` 支持 `{ label, width }` 表头配置，家长管理列表开始使用列宽配置，改善长表格横向滚动和操作列固定后的可读性。

## 2026-06-08 补充：教师管理列表字段顺序、脱敏与列宽对齐
- 教师管理列表继续按 `/admin-api/eduadmin/teacher-ele/page` 字段口径调整列：教师ID、用户ID、昵称、真实姓名、联系电话、微信号、教师类型、授课方式、出生年份、教龄、课时费、机构课时费、授课区域、教学科目、认证状态、账号状态、订单数、浏览数、加入日期、操作。
- 新增 `lbMaskPhone()`，列表联系电话按线上后台常见方式隐藏中间四位，例如 `130****0000`。
- 教师管理列表开始使用 `{ label, width }` 列宽配置，增强长表格横向滚动和固定操作列可读性。

## 2026-06-08 补充：需求单管理列表字段顺序与列宽对齐
- 需求单管理列表继续按 `/admin-api/eduadmin/demand-ele/page` 字段口径扩展列表列：需求ID、需求编号、家长ID、家长/手机号、学生ID、学生姓名、科目编码、需求科目、年级编码、学生年级、学段、授课方式、省/市/区编码、授课城市、授课地址、预算范围、信息费、信息费订单号、需求状态、浏览/报名、成功报名、并发数、发布时间、操作。
- 列表使用 `{ label, width }` 表头配置，长字段横向滚动展示，操作列继续固定在右侧，避免详情字段常驻挤占列表主体。

## 2026-06-08 修正：需求单管理列表按线上可见列还原
- 经线上页面 `/lbedu/order/demand-ele` 可见结构核对，需求单管理列表实际展示精简业务列：需求编号、家长姓名、手机号、学生姓名、学生年级、需求科目、授课方式、授课区域、预算范围、发布时间、需求状态、申请数、浏览数、操作。
- 已将本地列表从接口全字段长表修正为线上可见表格；科目编码、年级编码、学段、信息费订单号、成功报名数、并发数等接口字段继续保留在详情/编辑弹窗中，不作为列表常驻列展示。

## 2026-06-08 修正：家长管理与教师管理列表按线上可见列还原
- 经线上页面 `/lbedu/users/parent-ele` 核对，家长管理列表实际展示：手机号、姓名、昵称、孩子数量、是否VIP、VIP过期时间、累计发单数、累计消费、注册时间、状态、操作；本地已移除家长ID、用户ID、微信号、地区、默认孩子ID、订单数、完成订单、最近需求时间、导出和编辑等非线上列表常驻项。
- 经线上页面 `/lbedu/users/teacher-ele` 核对，教师管理列表实际展示：昵称、姓名、联系电话、教学科目、加入日期、认证状态、操作；本地已移除教师ID、用户ID、微信号、教师类型、授课方式、出生年份、教龄、课时费、机构课时费、授课区域、账号状态、订单数、浏览数等非线上列表常驻项。
- 上述技术字段继续保留在详情/编辑弹窗中，用于承载接口字段和功能信息，不在列表页过度展示。

## 2026-06-08 修正：撮合单与VIP订单列表按线上可见列还原
- 经线上页面 `/lbedu/order/matching-order-ele` 核对，撮合单管理列表实际展示：订单编号、家长/手机、学生/教师、科目/方式、信息费、状态、来源、创建时间、操作；本地已移除订单ID、需求单编号、老师手机号、课时费、信息费订单号、导出、修改、退款等非线上列表常驻项。
- 经线上页面 `/lbedu/order/vip-order-ele` 核对，VIP订单管理列表实际展示：订单编号、家长姓名、VIP类型、VIP价格、实付金额、支付状态、支付渠道、支付时间、VIP开始时间、VIP到期时间、创建时间、操作；本地已移除订单ID、家长ID、支付订单号、导出等非线上列表常驻项。
- 订单详情、修改、退款、支付原始字段继续保留在弹窗中，不在列表页过度展示。

## 2026-06-08 修正：平台配置与评价管理按线上可见结构补齐
- 经线上页面 `/lbedu/config/sys-config-ele` 核对，平台参数配置筛选区应包含配置分组、参数名称、键值类型、参数键名、参数键值、默认值、排序、变量描述、是否为系统默认、状态、创建时间；本地已补齐缺失筛选项，列表表头保持线上一致。
- 经线上页面 `/lbedu/content/evaluation-ele` 核对，评价管理筛选区应覆盖订单、家长、教师、评分、标签、评价内容、匿名/展示、教师回复、创建时间等字段；本地已补齐筛选项。
- 评价管理列表操作按线上收敛为“删除”，移除列表常驻“查看详情”；是否匿名、是否展示按线上原始 0/1 口径展示。
- 同步修复撮合单与 VIP 订单筛选区中 `lbInput(...)` 被写成字面文本的隐藏问题，确保页面实际渲染为表单控件。

## 2026-06-08 修正：分销管理与支付订单按线上可见结构还原
- 经线上菜单权限核对，分销真实路由为 `/lbedu/distribution/distribution-rule-ele`、`/lbedu/distribution/distribution-user-ele`、`/lbedu/distribution/distribution-lead-ele`，支付订单真实路由为 `/pay/order`。
- 分销规则管理列表按线上显示规则编码、规则名称、触发身份、事件类型、返佣层级、地域范围、计算模式、返佣比例、固定返佣、优先级、状态、开始/结束时间、操作；操作保留编辑和启用/禁用。
- 分销账号列表按线上移除账号ID、关联家长、关联老师、查看详情等非可见列，保留平台用户ID、邀请码、上级用户ID、佣金、团队人数、最近绑定/返佣时间、状态、备注、创建时间。
- 分销线索管理按线上移除线索编号、客户用户ID、客户资料ID、目标类型等技术列，保留线索ID、归属人、客户信息、来源/线索类型、状态、目标对象、跟进内容、转化/创建时间、查看详情。
- 支付订单按线上移除支付订单ID、商户订单号、应用名称、支付主题、用户IP、成功时间、渠道订单号等长表字段，列表展示编号、支付金额、退款金额、手续金额、订单号、支付状态、支付渠道、支付时间、支付应用、商品标题、操作。

## 2026-06-08 修正：家长详情弹窗按线上可见结构还原
- 经线上 `/lbedu/users/parent-ele` 第一行“查看”实际弹窗核对，家长详情弹窗顶部展示家长姓名和 `ID: PA990017`，不展示接口型家长ID/用户ID长字段。
- 本地家长详情已收敛为线上结构：基础信息、学生信息、需求与运营数据、底部 Tab（需求记录、订单记录、邀请关系）和默认空需求记录表。
- 禁用操作线上确认框本轮未稳定捕获到，为避免误提交线上数据，本地继续保留非提交型“操作确认”弹窗。

## 2026-06-08 修正：教师详情弹窗按线上可见结构还原
- 经线上 `/lbedu/users/teacher-ele` 第一行“详情”实际弹窗核对，教师详情弹窗顶部展示教师头像、真实姓名、昵称、教师类型、教龄和注册日期，不展示接口型教师ID/用户ID长字段。
- 本地教师详情已收敛为线上结构：基础信息、教学信息、运营数据、系统日志、底部 Tab（订单记录、用户评价、资质证件）和默认空订单记录表。
- 教师修改弹窗本轮未稳定捕获到线上结构，为避免误提交线上编辑数据，本地暂保留原有可编辑字段，后续在能稳定捕获时继续校准。

## 2026-06-08 修正：教师入驻审核详情按线上可见结构还原
- 经线上 `/lbedu/audit/teacher-audit-record-ele` 第一行“详情”实际弹窗核对，审核详情并不展示接口元信息长表，而是按审核信息、基本信息、授课信息、价格方案、自我介绍分组展示。
- 本地审核详情已移除审核ID、教师ID、审核类型值、审核状态值、头像URL、证明材料URL、省市区编码、授课时间标记等接口字段常驻展示，收敛为线上可见字段。
- 通过/驳回弹窗仍保留非提交型确认和驳回原因输入，避免对线上数据产生副作用。

## 2026-06-08 修正：需求单详情按线上可见结构还原
- 经线上 `/lbedu/order/demand-ele` 第一行“查看详情”实际弹窗核对，需求详情按需求信息、家长信息、学生信息、课程信息、预算信息、时间安排、教师要求、状态信息分组展示。
- 本地需求详情已移除需求ID、家长ID、学生ID、科目编码、年级编码、授课方式值、省市区编码、信息费订单号、性别要求值、并发处理数等接口字段常驻展示。
- 取消需求继续保留非提交型“操作确认”弹窗，避免对线上数据产生副作用。

## 2026-06-08 修正：撮合单与 VIP 订单详情按线上可见结构收敛
- 撮合单详情从接口字段长表收敛为线上可见业务结构：顶部订单编号与状态、订单基础信息、授课信息、订单日志、家长信息、学生信息、家教信息、支付信息。
- 已移除撮合单详情常驻展示中的订单ID、需求ID、城市编码、倍率、信息费订单号、老师收入、平台费用、企微群ID、取消类型值等接口型字段，避免后台详情页像接口调试面板。
- VIP订单详情从接口字段长表收敛为线上可见业务结构：订单基础信息、VIP权益、支付信息；字段展示为订单编号、家长姓名、VIP类型、VIP价格、实付金额、支付渠道、支付时间、VIP开始/到期时间等。
- 已移除 VIP 详情常驻展示中的 `id/orderNo/parentId/createTime/vipType/vipPrice/payAmount/payStatus/payOrderNo/payTime` 这类字段名后缀，保留业务含义，不暴露接口字段名。
- 补充详情弹窗视觉样式：顶部状态条、双栏信息卡、空日志行、小型教师卡片、查看详情文字按钮。
- 同步更新第二批运行时验证脚本断言，验证目标改为线上可见业务字段，而不是旧接口字段。

## 2026-06-08 修正：评价详情与支付订单详情去接口字段后缀
- 经线上 `/lbedu/content/evaluation-ele` 与 `/pay/order` 页面核对，评价管理列表本身保留主键ID、关联订单ID等字段口径；本轮不改列表，仅将详情/删除确认弹窗从英文接口字段名后缀收敛为中文业务字段。
- 评价详情弹窗改为：顶部订单编号与展示状态、订单与评价对象、评价内容、教师回复，不再展示 `orderId/orderNo/parentId/teacherId/ratingScore/isVisible/replyTime` 等字段名后缀。
- 评价删除确认弹窗改为确认文案 + 主键ID、订单编号、家长、教师、评分、展示状态，不再展示接口权限说明。
- 经线上 `/pay/order` 页面核对，支付订单列表核心字段为编号、支付金额、退款金额、手续金额、订单号、支付状态、支付渠道、支付时间、支付应用、商品标题；本地支付订单详情同步收敛到这些业务信息。
- 支付订单详情弹窗改为：顶部支付订单与状态、订单金额、订单号、支付信息；移除应用ID、扩展单ID、渠道ID、渠道用户ID、状态值、金额分单位字段、回调地址、过期时间等接口调试字段。
- 同步更新第二批/第三批运行时验证脚本断言，验证目标改为线上业务字段。

## 2026-06-08 修正：平台配置与分销弹窗去接口字段后缀
- 经线上 `/lbedu/config/sys-config-ele`、`/lbedu/distribution/distribution-rule-ele`、`/lbedu/distribution/distribution-user-ele`、`/lbedu/distribution/distribution-lead-ele` 页面核对，列表字段以中文后台字段为主；本轮不改列表结构，重点清理弹窗里的接口字段名后缀。
- 平台配置弹窗改为配置ID、配置分组、参数名称、键值类型、参数键名、排序、是否为系统默认、状态、参数键值、默认值、变量描述、创建时间，不再展示 `id/group/name/type/key/value/defaultValue/isDefault/status/createTime` 等后缀。
- 分销规则新增/编辑弹窗改为规则编码、规则名称、触发身份、事件类型、返佣层级、地域范围、计算模式、返佣比例、固定返佣、最小基数、最高返佣、优先级、开始/结束时间、备注，不再展示触发身份值、地域类型值、省市区编码、计算模式值、基数类型值等接口字段。
- 分销账号详情弹窗改为顶部邀请码与平台用户ID、账号基础信息、佣金数据、团队数据，移除账号ID、字段英文名、关联老师/家长接口对象信息。
- 分销线索详情弹窗改为顶部客户与线索状态、客户信息、来源与状态、跟进记录，移除 `leadNo/ownerMemberUserId/customerMemberUserId/customerAvatarUrl/leadStatusName/targetId/latestContent` 等字段名后缀。
- 同步更新第二批/第三批运行时验证脚本断言，并增加本轮五个弹窗英文接口字段残留扫描。

## 2026-06-08 修正：剩余编辑/确认弹窗去技术字段
- 家长编辑弹窗从接口字段表收敛为基础资料、孩子与家庭、会员与运营、后台备注；移除家长ID、用户ID、头像URL、省市区编码、GeoHash、权限说明等技术字段。
- 教师编辑弹窗从接口字段表收敛为基础信息、教师类型与地区、教学信息、介绍与状态；移除教师ID、用户ID、头像URL、省市区编码、授课科目编码、授课区域编码、技术变量名等字段。
- 需求单编辑弹窗从接口字段表收敛为需求基础信息、家长与学生、课程与区域、预算与时间、老师要求与备注；移除需求ID、家长ID、学生ID、状态值、年级/科目/地区编码、信息费订单号等字段。
- 撮合单修改弹窗从全量接口字段表收敛为订单基础、老师与家长、课程与费用、履约与时间、取消与备注；移除订单ID、需求ID、老师ID、家长ID、城市编码、信息费订单号、企微群ID、取消类型值、接口权限说明。
- 撮合单退款弹窗改为业务确认结构：退款对象、费用核对、退款说明；移除订单ID、需求ID、家长ID、老师ID、订单状态值、取消类型值、接口调用说明。
- 导出确认弹窗改为导出范围、导出格式、导出内容、处理方式；移除“权限来源/线上 export 权限”。
- 同步更新运行时验证脚本，并增加六个弹窗技术字段残留扫描。

## 2026-06-08 修正：后台首页与顶部页签覆盖当前线上入口
- 审计发现领榜后台导航已覆盖线上 12 个核心入口，但后台首页快捷入口仍只展示家长管理、教师管理、教师入驻审核、需求单管理 4 个页面，顶部页签也只展示 5 个入口，状态表达滞后。
- 已将后台首页快捷入口扩展为 12 个线上已覆盖页面：家长管理、教师管理、需求单管理、撮合单管理、VIP订单管理、教师入驻审核、平台参数配置、评价管理、分销规则管理、分销账号列表、分销线索管理、支付订单。
- 后台首页指标卡同步改为用户业务管理、业务订单管理、运营与审核、分销与支付四类，避免仍像早期第一批还原状态。
- 顶部页签扩展为后台首页 + 12 个线上核心入口，便于直接切换当前已还原页面。
- 新增 `/tmp/k12_validate_lbedu_home_coverage.js` 验证后台首页和页签覆盖 12 个线上入口。

## 2026-06-08 修正：家长/教师管理筛选区真实控件恢复
- 细查 12 个领榜后台页面的筛选区、表头和操作列时发现，家长管理与教师管理的筛选项被错误拼接为字面文本，页面实际未渲染真实输入控件。
- 家长管理筛选区已恢复为真实控件：关键词、注册状态、VIP状态、注册时间，并保留重置、搜索、收起操作。
- 教师管理筛选区已恢复为真实控件：关键词、状态，并保留重置、搜索、收起操作。
- 同步更新 `/tmp/k12_validate_runtime.js`，新增家长/教师管理筛选项断言，防止后续回退为文本。

## 2026-06-08 修正：领榜后台列表表格工具栏覆盖对齐
- 对照线上家长管理与教师管理页面，线上表格标题右侧均包含搜索、刷新、全屏、列设置图标按钮。
- 本地已将此前缺少工具栏的列表页补齐 `lbToolbar()`：家长管理、需求单管理、撮合单管理、VIP订单管理、分销账号列表、分销线索管理。
- 教师管理、教师入驻审核、平台参数配置、评价管理、分销规则管理、支付订单原已具备工具栏，本轮保持不变。
- 新增 `/tmp/k12_validate_lbedu_table_toolbars.js`，验证 12 个领榜后台列表页均包含搜索、刷新、全屏、列设置图标区域。

## 2026-06-08 修正：家长/教师管理首屏样例数据对齐
- 对照线上 `/lbedu/users/parent-ele` 首屏，补充本地家长列表样例数据到更接近线上首屏：刘震、用户_4305420710、用户_3985070574、用户_4114496173、用户_1162708390、马绵、子薇妈妈、啊啊啊、冯、用户_1909108208、孙志强、马晓红等。
- 家长列表累计消费展示从 `0.00` 收敛为线上首屏整数样式 `0`，VIP 家长马晓红保留 `1298` 样例。
- 对照线上 `/lbedu/users/teacher-ele` 首屏，教师管理科目展示从英文编码 `math,physics,chemistry` 修正为中文 `数学，物理，化学`。
- 新增 `lbSubjectText()` 对科目编码做中文化兜底，避免后续教师列表再次出现英文科目编码。
- 同步更新 `/tmp/k12_validate_runtime.js`，新增家长首屏样例、教师中文科目断言。

## 2026-06-08 核对：领榜后台操作列保留规则
- 线上 `/lbedu/users/parent-ele` 经 DOM 与按钮坐标核对，家长管理存在右侧固定操作列，行内按钮为“查看”“禁用”；本地保留家长管理操作列是正确的，不应删除。
- 教师管理线上存在“详情”“修改”“禁用”操作；本地保持一致。
- 新增 `/tmp/k12_validate_lbedu_action_columns.js`，验证家长、教师、需求、撮合、VIP、审核、配置、评价、分销规则、分销线索、支付订单等页面的操作列和按钮语义。
- 分销账号列表线上无操作列，本地验证脚本明确断言不出现“操作/查看详情”，避免误加。

## 2026-06-08 修正：真实浏览器环境下分页显示异常
- 通过本地 HTTP 服务 `http://127.0.0.1:8765/` 打开主原型，并在真实浏览器环境切换到领榜后台，确认 iframe 内后台首页可正常渲染。
- 真实浏览器验证发现后台首页 12 条快捷入口的分页仍显示硬编码页码 `1 2 3 ... 1`，与线上分页逻辑不符。
- 已将 `lbPager()` 从固定页码改为按总条数动态生成：总页数为 1 时只显示当前页，并禁用下一页；小于 20 条时显示真实范围，例如 `显示 1-12 条，共 12 条`。
- 更新 `/tmp/k12_validate_lbedu_visual.js`，去除旧的硬编码 `显示 1-20 条` 断言，改为保留分页结构关键类校验。
- 真实浏览器复验：领榜后台首页分页已显示为 `显示 1-12 条，共 12 条`，不再出现多余页码。

## 2026-06-08 修正：弹窗详情主按钮关闭行为
- 真实浏览器验证家长管理“查看”详情弹窗时发现，点击底部主按钮“关闭”会触发全局异步完成状态，而不是关闭弹窗。
- 已修复领榜后台通用弹窗生成逻辑：当主按钮文案为“关闭”时，点击只执行 `closeModal()`；保存、删除、退款、审核等业务主按钮仍执行提交反馈。
- 真实浏览器复验结果：家长详情弹窗可打开，点击“关闭”后仅关闭弹窗且仍停留在家长列表；随后“禁用”按钮可正常打开“操作确认”弹窗。
- 全量结构与回归脚本均通过。

## 2026-06-08 修正：只读详情弹窗主按钮统一为关闭
- 已将企微监管详情、异常详情、推送详情、撮合单详情、VIP订单详情、评价详情、分销账号详情、分销线索详情、支付订单详情等只读详情弹窗的主按钮统一改为“关闭”。
- 只读详情弹窗点击主按钮只关闭弹窗，不再触发异步提交反馈，避免把查看类动作误表达为保存或确认。
- 保留审核、驳回、禁用、退款、删除、发送、重新开放等业务操作弹窗的提交反馈逻辑。
- 全量结构检查与领榜后台运行时验证脚本已通过，未恢复内部重复全端导航和页面状态测试工具。

## 2026-06-08 修正：需求单管理首屏数据按线上页面补齐
- 经线上 `/lbedu/order/demand-ele` 只读核对，需求单管理列表字段为：需求编号、家长姓名、手机号、学生姓名、学生年级、需求科目、授课方式、授课区域、预算范围、发布时间、需求状态、申请数、浏览数、操作。
- 本地需求单管理表头已保持上述线上结构，本轮将首屏样例数据从 4 条扩展到 20 条，补齐线上可见的 `MOCK_DEMAND_020` 至 `MOCK_DEMAND_004` 等样例。
- 修正首两条线上可见发布时间为 `2026-05-15 18:25:45`、`2026-05-14 21:47:24`，并统一线上文案“在线授课”“学员上门”等可见授课方式。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。

## 2026-06-08 修正：家长管理首屏数据按线上 20 条补齐
- 经线上 `/lbedu/users/parent-ele` 只读核对，家长管理列表首屏为 20 条/页，表头为手机号、姓名、昵称、孩子数量、是否VIP、VIP过期时间、累计发单数、累计消费、注册时间、状态、操作。
- 本地家长管理表头已保持线上结构，本轮将首屏样例从 12 条扩展为 20 条，补齐赵文彬、吴芳、周文博、黄雪梅、杨伟东、陈丽华、刘志明、赵秀英等线上可见样例。
- 保持线上可见筛选项：关键词、注册状态、VIP状态、注册时间；保持操作按钮“查看/禁用”和总数 42 条。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。

## 2026-06-08 修正：教师管理首屏数据按线上 20 条补齐
- 经线上 `/lbedu/users/teacher-ele` 只读核对，教师管理列表首屏为 20 条/页，表头为昵称、姓名、联系电话、教学科目、加入日期、认证状态、操作。
- 本地教师管理表头、筛选项、导出按钮、行内“详情/修改/禁用”操作已保持线上结构，本轮将首屏样例从 3 条扩展为 20 条。
- 补齐王老师、周老师、吕老师、魏老师、于老师、刘老师、张老师、马老师、金老师、赵老师、杜老师等线上可见样例，并保留线上可见科目文案中的 `companion`、`preschool` 等原始显示。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。

## 2026-06-08 修正：教师入驻审核列表按线上真实路由与首屏对齐
- 经线上菜单进入真实路由 `/lbedu/audit/teacher-audit-record-ele` 只读核对，原猜测 `/lbedu/audit/teacher-ele` 为 404。
- 教师入驻审核线上列表标题为“家教审核列表”，表头为审核类型、教师姓名、手机号、申请时间、审核状态、审核人、审核时间、操作；本地已移除旧的审核ID、更新人两列。
- 审核类型文案从“首次入驻”修正为线上可见“初次注册”，并补齐 20 条首屏样例，包含刘震、司晓君、系统管理员、dsda、田书培等记录。
- 行内操作按线上状态区分：待审核显示“详情/通过/驳回”，已审核记录只显示“详情”。
- 同步更新 `/tmp/k12_validate_runtime.js` 的教师入驻审核断言为线上口径；全量结构检查与领榜后台运行时验证脚本已通过。

## 2026-06-08 修正：VIP订单管理按线上真实路由与首屏对齐
- 经线上菜单进入真实路由 `/lbedu/order/vip-order-ele` 只读核对，原猜测 `/lbedu/order/vip-ele` 为 404。
- VIP订单管理线上筛选项为家长姓名、订单编号、支付状态、时间范围；表头为订单编号、家长姓名、VIP类型、VIP价格、实付金额、支付状态、支付渠道、支付时间、VIP开始时间、VIP到期时间、创建时间、操作。
- 本地已修正 VIP 订单列表数据映射：价格字段不再重复拼接“元”，家长姓名列按线上空值样式保留为空，支付成功记录显示支付渠道和会员起止时间。
- 本地 VIP 订单首屏样例从 5 条扩展为 20 条，补齐 `VIP2052947503906832384` 到 `VIP2048290341280649216` 等线上可见订单，列表总数保持 78 条。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。

## 2026-06-08 修正：撮合单管理按线上真实路由与首屏对齐
- 经线上菜单进入真实路由 `/lbedu/order/matching-order-ele` 只读核对，撮合单管理筛选项为订单编号、家长姓名、手机号、订单状态、创建时间区间。
- 撮合单线上表头为订单编号、家长/手机、学生/教师、科目/方式、信息费、状态、来源、创建时间、操作；本地已保持该结构。
- 本地撮合单首屏样例从 5 条扩展为 20 条，补齐 `MOCK_ORDER_020` 到 `MOCK_ORDER_001` 等线上可见订单，列表总数保持 88 条。
- 列表渲染按线上收敛：科目/方式列只显示科目文本，信息费按 `¥500` 这类整数样式展示，不再混入年级或多层金额。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。

## 2026-06-08 修正：支付订单按线上真实路由与首屏对齐
- 经线上真实路由 `/pay/order` 只读核对，支付订单筛选项为应用编号、支付渠道、商户单号、支付单号、渠道单号、支付状态、创建时间。
- 支付订单线上表头为编号、支付金额、退款金额、手续金额、订单号、支付状态、支付渠道、支付时间、支付应用、商品标题、操作；本地已保持该结构和导出/详情操作。
- 本地支付订单首屏样例从 5 条扩展为 20 条，补齐编号 `750` 至 `731` 等线上可见订单，列表总数保持 449 条。
- 列表渲染改为直接使用线上显示值：支付金额为 `50000.00/10000.00/1.00` 等，订单号为“商户 106”等，支付成功行显示“微信小程序支付”和“教育平台vip/三年VIP”。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。

## 2026-06-08 修正：平台参数配置首屏按线上对齐
- 经线上真实路由 `/lbedu/config/sys-config-ele` 只读核对，平台参数配置筛选项为配置分组、参数名称、键值类型、参数键名（业务唯一标识）、参数键值、默认值、排序、变量描述、是否为系统默认、状态、创建时间。
- 平台配置线上表头为配置ID、配置分组、参数名称、键值类型、参数键名（业务唯一标识）、参数键值、默认值、排序、变量描述、是否为系统默认、状态、创建时间、操作；本地已保持该结构和新增/修改操作。
- 本地平台配置首屏样例从 5 条扩展为 20 条，补齐配置ID 40、39、38、37、36、35、28、27 至 15 等线上可见记录，列表总数保持 34 条。
- 保留线上可见的学段科目对照 JSON、客服配置、系统协议、客服二维码、首单奖励有效期等配置内容，避免再显示成接口调试说明。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。

## 2026-06-08 修正：评价管理首屏按线上对齐
- 经线上真实路由 `/lbedu/content/evaluation-ele` 只读核对，评价管理筛选项为关联订单ID、订单编号（冗余）、评价人（家长）ID、家长姓氏（冗余）、被评价人（教师）ID、教师姓名（冗余）、评分、评价标签（逗号分隔）、评价内容、是否匿名、是否展示、教师回复内容、教师回复时间、创建时间。
- 评价管理线上表头为主键ID、关联订单ID、订单编号（冗余）、评价人（家长）ID、家长姓氏（冗余）、被评价人（教师）ID、教师姓名（冗余）、评分、评价标签（逗号分隔）、评价内容、是否匿名、是否展示、教师回复内容、教师回复时间、创建时间、操作；本地已保持该结构和批量删除/删除操作。
- 本地评价管理样例从 4 条扩展为线上全部 10 条，补齐主键ID 13 至 4 的评价记录。
- 保留线上可见长评价内容，例如吕子薇、马绵、测、333 等教师评价文本，避免列表内容被简化成占位句。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。

## 2026-06-08 修正：分销规则管理按线上完整 9 条对齐
- 经线上真实路由 `/lbedu/distribution/distribution-rule-ele` 只读核对，分销规则筛选项为规则编码、规则名称、触发身份、地域范围、事件类型、状态。
- 分销规则线上表头为规则编码、规则名称、触发身份、事件类型、返佣层级、地域范围、计算模式、返佣比例、固定返佣、优先级、状态、开始时间、结束时间、操作；本地已保持该结构和新增/编辑/启用/禁用操作。
- 本地分销规则补齐线上第 9 条 `DEFAULT_PARENT_REGISTER_L1_CN / 全国家长注册一级返佣示例`，总数与线上一致为 9 条。
- 固定返佣继续按线上显示去除 `.00`，例如 60、20、10、5；启用规则显示“禁用”，禁用规则显示“启用”。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。

## 2026-06-08 修正：分销账号列表首屏按线上 20 条对齐
- 经线上真实路由 `/lbedu/distribution/distribution-user-ele` 只读核对，分销账号筛选项为平台用户ID、邀请码、上级用户ID、状态、创建时间、最近绑定时间。
- 分销账号线上表头为平台用户ID、邀请码、上级用户ID、累计佣金、已结算佣金、可提现佣金、已提现佣金、冻结佣金、直属人数、间接人数、团队人数、最近绑定时间、最近返佣时间、状态、备注、创建时间；本地已保持无操作列结构。
- 本地分销账号首屏样例从 4 条扩展为 20 条，补齐 `900017/TTZG9IOP` 到 `900004/TESTD004` 等线上可见记录，列表总数保持 23 条。
- 补齐特殊字段：`2054/IULKSICW` 的最近绑定时间，以及 `900006/900005/900004` 的分销账户备注。
- 全量结构检查与领榜后台运行时验证脚本已通过，未改动领榜家长端、领榜教师端和优坐标各端。
