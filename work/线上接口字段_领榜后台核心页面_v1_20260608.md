# 线上接口字段_领榜后台核心页面_v1_20260608

## 背景
继续将线上领榜教育管理系统后台页面 1:1 还原到本地离线高保真原型。

## 目标
记录已通过线上接口读取到的核心页面字段和样例数据，用于后续继续校准本地原型。

## 输入来源
- 线上地址：`https://lbedu.youzuobiao.cn/#/lbedu/users/parent-ele`
- 租户：`tenant-id: 1`
- 接口前缀：`/admin-api`
- 本地响应缓存：`/tmp/lbedu_core_api_responses.json`

## 关键结论
- 线上真实菜单包含：家长管理、教师管理、需求单管理、撮合单管理、VIP订单管理、教师入驻审核、平台参数配置、评价管理、分销规则管理、分销账号列表、分销线索管理、支付订单。
- 本轮已重点校准：分销规则管理、支付订单。
- 支付订单真实接口为 `/admin-api/pay/order/page`，字段不是之前临时设定的“业务订单号/微信支付回传订单号”口径，而是支付平台通用字段：支付订单ID、商户订单号、应用名称、支付主题、支付内容、金额、状态、用户IP、创建时间、成功时间、渠道订单号。
- 分销规则真实接口为 `/admin-api/eduadmin/distribution-rule-ele/page`，规则编码包括 `TEST_TEACHER_ORDER_L1_CN`、`TEST_PARENT_TRIAL_L1_SH` 等。

## 待确认问题
- 是否继续逐页按接口真实字段替换所有本地模拟数据。
- 是否需要同步线上页面截图做像素级视觉校准。

## 风险与依赖
- 线上接口数据可能随测试库变化，后续校准应以最新接口为准。
- 离线原型仍为静态模拟，不接真实接口。

## 下一步动作
- 继续校准教师入驻审核、平台参数配置、评价管理、撮合单管理等页面的真实字段和样例数据。
- 每轮修改后继续运行已有四批校验与菜单覆盖校验。

## 2026-06-08 补充：教师入驻审核校准
- 已将教师入驻审核列表改为真实接口字段口径：审核ID、审核类型、教师姓名、手机号、申请时间、审核状态、审核人、审核时间、更新人、操作。
- 已将审核详情弹窗按 `contentSnapshot` 关键字段补充：昵称、真实姓名、出生年份、微信、所在城市、家庭住址、毕业院校、专业、教师类型、授课方式、授课科目、授课年级、授课区域、教龄、授课时间、课时费用、试讲类型、证明材料。
- 本轮恢复了第二批页面常量，避免函数替换时影响撮合单、VIP订单、平台配置、评价管理页面。

## 2026-06-08 补充：撮合单与 VIP 订单校准
- 撮合单管理已按 `/admin-api/eduadmin/matching-order-ele/page` 真实字段扩展：订单ID、订单编号、需求单编号、家长/手机、老师/手机、科目年级、课时费、信息费、信息费订单号、状态、创建时间。
- 撮合单详情已补充：需求ID、老师/家长联系方式、城市、倍率、确认时间、取消类型、取消原因等。
- VIP订单管理已按 `/admin-api/eduadmin/vip-order-ele/page` 真实字段扩展：订单ID、订单编号、家长ID、VIP类型、VIP价格、实付金额、支付订单号、支付状态、支付时间、VIP开始时间、VIP到期时间、创建时间。

## 2026-06-08 补充：用户业务与需求单校准
- 家长管理已按 `/admin-api/eduadmin/parent-ele/page` 真实字段扩展：家长ID、用户ID、手机号、姓名、昵称、微信、孩子数量、VIP状态、累计订单、完成订单、累计消费、注册时间、账号状态。
- 教师管理已按 `/admin-api/eduadmin/teacher-ele/page` 真实字段扩展：教师ID、用户ID、昵称、姓名、联系电话、教师类型、授课方式、教学科目、课时费、机构课时费、订单数、浏览数、认证状态。
- 需求单管理已按 `/admin-api/eduadmin/demand-ele/page` 真实字段扩展：需求ID、需求编号、家长ID、家长/手机号、学生ID、学生姓名、年级、科目、授课方式、城市、授课区域、预算范围、需求状态、申请数、浏览数。

## 2026-06-08 补充：分销账号与分销线索校准
- 分销账号列表已按 `/admin-api/eduadmin/distribution-user-ele/page` 真实字段扩展：账号ID、平台用户ID、邀请码、上级用户ID、关联家长、关联老师、累计佣金、已结算佣金、可提现佣金、已提现佣金、冻结佣金、直属人数、间接人数、团队人数、最近绑定时间、状态、备注、创建时间。
- 分销线索管理已按 `/admin-api/eduadmin/distribution-lead-ele/page` 真实字段扩展：线索ID、线索编号、归属人ID、客户用户ID、客户资料ID、客户姓名、客户手机号、客户身份、线索类型、线索状态、目标类型、目标对象、最近跟进时间、转化时间、创建时间。
- 分销线索详情已补充关系层级、来源类型、目标ID、最近跟进内容、备注等真实接口字段。

## 2026-06-08 补充：平台配置与评价管理校准
- 平台参数配置已按 `/admin-api/eduadmin/config-ele/page` 真实字段补齐：配置ID、配置分组、参数名称、键值类型、参数键名、参数键值、默认值、排序、变量描述、是否系统默认、状态、创建时间。
- 平台配置修改弹窗已补齐配置ID、默认值、排序、创建时间等字段，保持列表页只展示筛选、表格、分页和行内操作。
- 评价管理已按 `/admin-api/eduadmin/evaluation-ele/page` 真实字段补齐：主键ID、关联订单ID、订单编号、家长ID/姓名、教师ID/姓名、评分、评价标签、评价内容、是否匿名、是否展示、教师回复内容、教师回复时间、创建时间。
- 评价管理新增“查看详情”弹窗，删除仍使用独立确认弹窗，不在列表页常驻展示详情或确认内容。

## 2026-06-08 补充：撮合单操作权限校准
- 根据线上权限 `eduadmin:matching-order-ele:update` 和 `eduadmin:matching-order-ele:refund`，撮合单管理列表操作从单一“详情”扩展为“详情 / 修改 / 退款”。
- 新增“修改撮合单”弹窗，覆盖订单状态、信息费订单号、确认时间、取消类型、取消原因、后台备注等可编辑字段。
- 新增“撮合单退款处理”弹窗，覆盖订单、家长、老师、信息费订单号、可退信息费、退款金额、退款原因、退款说明。
- 退款弹窗只做高保真原型交互，不调用真实支付退款接口。

## 2026-06-08 补充：家长与需求单更新权限校准
- 根据线上权限 `eduadmin:parent-ele:update`，家长管理列表新增“编辑”操作，并新增“编辑家长资料”弹窗。
- 家长编辑弹窗覆盖：家长ID、用户ID、手机号、家长姓名、昵称、微信号、年级、性别、家庭住址、学校、VIP状态、账号状态、后台备注。
- 根据线上权限 `eduadmin:demand-ele:update`，需求单管理列表新增“修改”操作，并新增“修改需求单”弹窗。
- 需求单修改弹窗覆盖：需求ID、需求编号、家长ID、家长姓名、手机号、需求状态、学生姓名、学生年级、需求科目、授课方式、城市、授课区域、授课地址、预算上下限、教师要求、后台备注。

## 2026-06-08 补充：导出权限入口校准
- 根据线上权限 `export`，已为家长管理、教师管理、需求单管理、撮合单管理、VIP订单管理、支付订单补齐“导出”入口。
- 导出按钮统一打开“导出确认”弹窗，展示导出范围、导出格式、权限来源和原型边界。
- 原型不生成真实下载文件，避免离线高保真页面产生副作用。

## 2026-06-08 补充：分销规则新增与编辑权限拆分
- 根据线上权限 `eduadmin:distribution-rule-ele:create` 和 `eduadmin:distribution-rule-ele:update`，分销规则管理已拆分“新增规则”和“编辑规则”两个弹窗。
- 顶部“新增规则”打开新增弹窗，行内“编辑”打开编辑弹窗，不再复用同一个“新增规则”弹窗。
- 编辑弹窗使用真实规则样例 `TEST_TEACHER_ORDER_L1_CN / 老师成单一级返佣`，与线上规则列表字段保持一致。

## 2026-06-08 补充：支付订单详情字段补齐
- 支付订单详情已按 `/admin-api/pay/order/page` 真实字段继续补齐：应用ID、支付订单号、扩展单ID、渠道费率、渠道费用、渠道ID、渠道编码、渠道用户ID、回调地址、过期时间。
- 详情弹窗重新分组为基础信息、支付内容、金额信息、渠道信息、回调与时间，提升与线上支付订单详情字段口径的一致性。
