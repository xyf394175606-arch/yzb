# 领榜家长端与教师端独立拆分原型说明

## 背景

在 `v1_20260722` 独立拆分原型的基础上，按统一原型工作台规范调整评审外壳。用户需要以左侧导航进入页面、中间查看业务原型、右侧阅读需求说明的形式评审家长端和教师端。

## 目标

- 保留家长端和教师端既有页面、mock 数据、端内导航、交互状态、权限样例和标注工具。
- 将评审外壳统一为顶部栏、左侧页面导航、中间业务原型、右侧“需求说明”四区结构。
- 左侧导航点击后同步切换中间页面、当前高亮和右侧需求说明。
- 不引入优坐标端、领榜后台或跨端切换入口。

## 输入来源

- `高保真原型_领榜家长端独立拆分_v1_20260722.html`
- `高保真原型_领榜教师端独立拆分_v1_20260722.html`
- `00_项目规则/组件库调用规范_基础组件与业务模板_v1_20260718.md`
- `00_项目规则/原型设计规范_工作台布局与弹窗交互_v1_20260718.md`
- `10_高保真原型/高保真原型_统一工作台外壳与左右导航模板_v1_20260718.html`
- 对话 `019f7352-7b4f-7a60-88ad-f1ce93632241` 已沉淀并落地到仓库的组件库调用规范

## 关键结论

- 本轮按“已有原型迭代”处理，不改写业务流程、信息架构、字段契约和上游需求文件。
- 家长端默认页面为“家长首页”；教师端默认页面为“最新家教订单”。
- 左侧导航保留端内业务页面入口；详情、评价、资料、创客中心等子页面使用所属主页面高亮规则。
- 右侧需求说明按当前页面展示页面目标、展示/字段规则、操作结果、状态/权限和待确认项；它与业务原型分离，不覆盖弹窗或手机画布。
- 在 `max-width:1180px` 隐藏右侧需求说明，在 `max-width:860px` 隐藏左右两侧并保留中间手机原型，作为移动评审差异。

## 覆盖页面与交互

### 家长端

- 页面：家长首页、请家教、了解老师、家教订单、订单详情、订单评价、订单资料、个人中心、消息、帮助中心、关于我们、老师详情、教师入驻、创客中心、会员中心、推广海报。
- 交互：左侧导航切页；首页快捷入口；学段/学科筛选；老师列表和详情；需求发布；订单详情、评价和资料；个人中心入口；底部 Tab；标注新增、搜索、拖动、编辑和导入导出。
- 权限：以家长角色 mock 数据展示；真实账户、订单和接口权限待上游确认。

### 教师端

- 页面：最新家教订单、我的接单、订单详情、订单评价、订单资料、个人中心、消息、帮助中心、关于我们、老师详情、教师入驻、创客中心、会员中心、推广海报。
- 交互：左侧导航切页；订单筛选和详情；接单反馈；评价和资料查看；教师入驻分步填写与提交审核；个人中心入口；底部 Tab；标注新增、搜索、拖动、编辑和导入导出。
- 权限：以教师角色 mock 数据展示；真实接单、审核和资料权限待上游确认。

## 状态说明

- 保留原型已有默认、选中、加载、空数据/无结果、订单状态、提交成功、审核中、提示和标注面板状态。
- 新增外壳状态：左侧当前页高亮、右侧说明同步、1180px 说明区隐藏、860px 单列移动评审。
- 业务弹窗和底部操作沿用原版本，未新增删除按钮或改写保存规则。

## 待确认问题

- 右侧说明中的真实接口字段、状态码、权限矩阵仍需由 `09_数据交互` 上游文档确认。
- 家长端“当老师/教师入驻”入口是否继续对家长角色展示，需要产品确认；本轮为保留既有原型而未移除。
- 桌面评审工作台是否需要将手机业务区替换为响应式业务页面，当前仍按既有手机原型展示。

## 风险与依赖

- 现有 HTML 内嵌图片与标注数据体积较大，后续修改应继续采用新版本文件，避免覆盖历史评审结果。
- 真实接口、登录态和权限接入后，右侧说明和 mock 状态需要同步回填。
- 浏览器本地存储可能保留旧版标注工具状态，切换版本时应按页面标题区分验证。

## 下一步动作

- 依据产品评审反馈逐页核对右侧规则与上游需求、流程、交互和数据交互文档。
- 对明确的业务规则变更回流对应上游阶段，不直接在高保真原型中替代结论。
- 若确认统一工作台为后续固定交付形态，再将相同外壳抽取为可复用片段。

## 自检结果

- 文件命名：通过，新增文件使用 `主题_调整内容_版本号_创建时间`，日期为 `20260723`。
- 历史文件：通过，`v1_20260722` 未修改。
- HTML 结构与脚本静态检查：通过，两个新版均可加载并执行，控制台无 error；两个页面均无 iframe，未发现优坐标、领榜后台或跨端切换入口。
- 桌面三栏验证：通过，家长端和教师端均显示左侧导航、中间手机业务原型、右侧“需求说明”；家长端点击“请家教”后页面、导航高亮和说明同步，教师端点击“我的接单”后页面、导航高亮和说明同步。
- 首屏验证：通过，家长端默认“家长首页”，教师端默认“最新家教订单”，业务内容非空。
- 1180px/860px 响应式规则：CSS 规则静态检查通过，已写入右侧说明区隐藏、左侧导航收缩和移动单列规则；当前 in-app Browser 的 viewport 覆盖未改变实际 `innerWidth`，因此未能在该后端执行真实尺寸截图验证，需在支持 viewport 覆盖的浏览器中复核。
- 重叠检查：默认桌面截图显示三栏区域均存在；标注工具按原版本继续使用浮动面板，可能覆盖业务区，这是既有评审工具层，不属于主业务界面。
- 当前任务分类：已有原型迭代；本轮未修改上游需求、业务流程、信息架构和数据契约。
